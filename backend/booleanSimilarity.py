class BooleanSimlarity:
    docs = []

    def invIndex(good_types, documents):
        invIndex = {}
        # for i, enumerate

    def booleanSim(query, good_types):
        # for q in query
        print()
